<%# ユーザー一覧ページ %>
<% provide(:title, 'ユーザーの一覧') %>
<h1>ユーザーの一覧</h1>

<%= will_paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate @users %>
